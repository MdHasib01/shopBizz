"use-client";
import useSidebar from "@/hooks/useSidebar";
import React from "react";

const SidebarWrapper = () => {
  //   const { activeSidebar, setActiveSidebar } = useSidebar();
  return <div>Sidebar Wrapper</div>;
};

export default SidebarWrapper;
